# CLAUDE.md

**El contexto técnico completo de este repo (arquitectura, stack, backend, convenciones de código) vive en `Raifeeer/Meridian/CLAUDE.md`, sección "Polaris-Web-Studio" -- no en este archivo.** Pedido explícito del usuario (8 de agosto): un solo `CLAUDE.md` real, para no depender de qué repos tenga habilitados cada sesión. Si estás trabajando acá y no tenés Meridian en el alcance de la sesión, leelo vía la Cloud Function `repo-file` (`https://repo-file-wdvfac6mgq-ue.a.run.app?repo=Meridian&path=CLAUDE.md`) antes de asumir que algo no existe.

## Rama de trabajo y commits

- Siempre trabaja directamente en la rama `main`.
- No crees ramas nuevas a menos que se indique explícitamente.
- Haz commit directo a `main` con todos los cambios.
- Usa mensajes de commit descriptivos en español.
- Un solo commit por tarea completada.
