# Ascenso implementation guide assets

The `es/` folders contain the current Spanish reference material. The `en/` folders are reserved for future translated/recreated assets.

Original references are stored separately from the user-approved annotated versions. Neither folder should be treated as a direct promise that Google will show the exact same interface.
